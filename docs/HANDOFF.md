# MOODEX — передача работы агенту

Документ для агента, который продолжает работу. Написан 30.07.2026 по итогам полного
рабочего дня. Читать целиком перед первым действием: половина текста — про ловушки, в
которые я уже попал, и повторять их незачем.

---

## 1. Что это и для кого

Система внутридневных торговых сигналов по Московской бирже. 48 бумаг, реалтайм-данные
Tinkoff, свой риск-контур, журнал прогнозов с оценкой по факту и накоплением уроков.

**Владелец торгует сам, живыми деньгами.** Счёт около 200 000 ₽, размер позиции
150 000–250 000 ₽, то есть работает почти на полном капитале. Его слова о приоритетах:

> «для меня важнее входы, тренды»
> «само направление тренда очень важно, если система определила тренд это уже хорошо,
> по тренду торговать куда лучше»
> «мне не нужно дешево, мне нужно качественно»

Про размер позиции он сказал, что это не так важно — риск его, и спорить не надо.
Считайте рублёвые числа от его реального размера, а не от лимитов риск-контура,
иначе ваши цифры будут не про его торговлю.

**Роль агента сейчас — аналитик.** Бюджет API Claude владелец не пополняет, поэтому
сигналы даёт агент напрямую, через `POST /api/analyst-signal`. Сигнал обязан попадать в
журнал: иначе это мнение в переписке, а не сделка.

---

## 2. Как работать практически

### Репозиторий

`ArtemNech-eng/modex`, ветка `main`. Публичный на чтение.

**Локального git-доступа на запись нет.** Пуш только через GitHub API:

```
ExecuteIntegration(action="github__push_files", paramsFile="/tmp/push.json")
```
где JSON содержит `{owner, repo, branch, message, files:[{path, content}]}`.

**Три правила, каждое оплачено ошибкой:**

1. **Никогда не делайте `git reset --hard` в том же блоке, где собираете пуш.** Я так
   затёр собственные несохранённые правки и отправил откаченные файлы — коммит
   `a8401d05` ушёл ПУСТЫМ, и я шесть минут ждал деплой кода, которого в нём не было.
2. **Перед пушем сверяйте содержимое с origin по хешам.** Если файл совпадает —
   коммит будет пустой. Проверка занимает пять строк и ловит первую ошибку.
3. **После пуша проверяйте, что в коммите есть файлы:**
   `GET /repos/ArtemNech-eng/modex/commits/<sha>` → поле `files` не должно быть пустым.

### Деплой

Coolify на сервере владельца, деплоит **автоматически на пуш в main**. Занимает одну-две
минуты. Прод: `http://qmo02nhtriftirhz8msvv7pw.176.124.200.67.sslip.io`

**Доступов к Coolify у агента нет.** Переменные окружения владелец правит сам — дайте
ему готовые строки. Значение булевых переменных должно быть словом `true`; `1`, `yes`,
`on` не работают (`os.getenv(...).lower() == "true"`).

**Как проверять, что деплой дошёл.** Не по `/api/stats` — он отвечает и на старом
контейнере, я на этом однажды сделал неверный вывод. Проверяйте **маркер новой
функции**: новое поле в ответе, новый маршрут (404 → 200), новое значение. Пока маркера
нет — код старый, сколько бы времени ни прошло.

При деплое контейнер перезапускается, и **всё, что запущено вручную, слетает**. У
наблюдателя сетапов и сканера есть счётчик проходов — если он сбросился, был перезапуск.

### Песочница агента

Python 3.9, **прод на 3.11**. Модули с синтаксисом 3.10+ (`X | None` в аннотациях)
локально не импортируются — например `src/analysis/geopolitics.py`. Значит локальный
прогон их молча пропускает. `sqlalchemy` в песочнице нет, поэтому `src/db.py` не
импортируется: тесты на функции БД пишите через чистые двойники, повторяющие логику, и
отдельным тестом сверяйте, что настоящая функция существует и содержит нужные куски.

---

## 3. Состояние на 30.07.2026, 18:30 МСК

| контур | состояние |
|---|---|
| наблюдатель сетапов (без Claude, бесплатный) | работает, интервал 5 мин, автостарт от `SETUP_WATCH_ENABLED=true` |
| сканер с подтверждением Claude (платный) | выключен, и это осознанно |
| контур обучения (оценка прогнозов) | работает, автостарт |
| сбор снимков рынка | работает, ~3200 событий в час |

Бюджет Claude: потрачено 10.97 ₽ из 100 ₽. Тестов **281** в 19 файлах.

**Сканер Claude намеренно выключен.** Причины: 29.07 он сжёг 91.92 ₽ из 100 ₽ за день, а
польза от его подтверждения **не измерена** — за всю историю у его сигналов нет ни одного
`confluence_score` и ни одного плана входа. Накопленная точность 29.4% относится к
правилам, а не к нему. Сначала неделя бесплатного контура, потом решение.

---

## 4. Что сделано 30.07 — двадцать коммитов

### Целостность данных (утро)

Порядок важен: каждая следующая находка была видна только после предыдущей правки.

| коммит | суть |
|---|---|
| `65df16d` | rvol сравнивал НЕПОЛНЫЙ сегодняшний бар с полными днями. Всплеск объёма 2.81× читался как 0.83, то есть «ниже среднего». Головная цифра теперь по последнему завершённому дню, темп дня — отдельной метрикой |
| `f8f638a` | `h, m = divmod(...)` затирало список `high` целым числом. **Каждый** расчёт контекста после 22:00 МСК падал с TypeError. Нашлось только когда восстановил полную копию репозитория: три тест-файла отсутствовали локально |
| `7da2b8a` | «Диапазон открытия» считался по первым свечам ОКНА ЗАГРУЗКИ, то есть по утренней сессии вместо основной. В 10:07 десять тикеров получили ложный пробой |
| `b07b8a3` | Три причины сразу: ISS отдаёт время ПО МОСКВЕ, а коллектор ставил UTC (+3 часа); интрадей запрашивался `from_date=вчера` и до сегодня не доходил; вчерашняя серия шла с пометкой «свежая». Добавлен заслон по возрасту свечей |
| `a6a78b2` | **Самое тяжёлое.** Рукописная таблица FIGI: 22 записи из 43 указывали на ЧУЖИЕ инструменты. HYDR получал свечи FEES, MAGN — свечи UPRO, MTSS — NLMK. По этим бумагам чужими были ВСЕ реалтайм-данные: свечи, VWAP, ATR, стакан, поток, цена. Таблица удалена, кэш заполняется только подтверждёнными API значениями |

### Настроение, новости, текст (день)

| коммит | суть |
|---|---|
| `1158d7a` | `volume_zscore` держался 1.91–2.45 у ВСЕХ 26 тикеров, 18 из 26 выше порога новостного события — при нуле текстовых сообщений. Считал снимки стакана и собственный разогрев: база `[1,2,3,4,5]` при счётчике 6 даёт ровно 1.90 |
| `44be6fa` | 2 из 5 RSS-источников мертвы молча (Финам 403, БКС анти-бот). Telegram: 21 канал «active», **0 событий** — статус брался из конфига, а не измерялся |
| `67afee8` | «Газпром нефть» → GAZP вместо SIBN. У префов SNGSP не было НИ ОДНОГО псевдонима. Тональность искалась подстрокой: «выгодно» содержит «дно», «support» содержит «up» |
| `689831b` | `has_fresh_news` не передавался НИОТКУДА — вход в детектор существовал, новости до него не доходили. Добавлена проверка причинности: новость должна стоять рядом со свечой выноса |
| `0370a54` | «На Мосбирже» по-русски значит «на бирже». Из семи привязок к MOEX пять были указанием места, точность 29% → 100% |
| `39745fa` | `ts` новости был временем СБОРА, а не публикации, и дедупликация жила в памяти процесса: 132 повтора из 200 записей, один заголовок вставлен 14 раз |

### Сетапы и журнал (вечер)

| коммит | суть |
|---|---|
| `6f6fb04` | У сетапа ORB не было срока годности: в 14:04 он выдавал «пробой» диапазона 10:00–10:30. Из 36 сетапов у 35 R/R был ниже 1.0. Добавлены срок годности 90 мин, порог R/R 1.5, согласие с VWAP |
| `200bb09` | `bid_ask` — это отношение ОБЪЁМОВ заявок, а не спред. Я принял 1.7 за «спред 1.70%» при реальных 0.0257% |
| `411eefc`, `fdbfffc`, `a0b38aa` | Журнал учился на цене, которой не было. Правка уровней до оценки + пересчёт снимка позиции + разделение «уровни решения неприкосновенны, снимок позиции — учётный факт» |
| `e63f740` | **Новый сетап: пробой внутридневной консолидации.** Закрывает дырку после полудня |
| `d8b45d3`, `d6434df` | **Быстрый наблюдатель сетапов без Claude**, интервал 5 минут |

---

## 5. Сетап пробоя консолидации — как он проверен

Это единственный сетап в системе, прошедший честную проверку. Методика — образец для
следующих, повторяйте её.

**Устройство.** Сжатие шести баров уже 1.2×ATR, пробой его верха, объём пробойного бара
от 1.5× среднего по сжатию, цена выше VWAP. Стоп под минимумом сжатия. Цель **вход + 2R**.

**Почему цель в риске, а не в ATR.** С целью 1.5×ATR планируемый R/R структурно
занижался: у победившего входа по Мечелу выходило 1.05 при фактическом ходе 4.03R — то
есть порог R/R ≥ 1.5 отклонил бы лучшую сделку дня. Цель в риске делает R/R равным двум
по построению, и остаётся один содержательный вопрос: достигается ли она.

**Проверка.** 12 ликвидных бумаг, 10-мин свечи, 18 торговых дней июля 2026, издержки
0.05% на круг, разбиение выборки по времени:

| ширина сжатия | всего | 1-я половина | 2-я половина | вывод |
|---|---|---|---|---|
| ≤2.0 ATR | 365 входов, +0.065R | **−0.098R** | +0.176R | знак меняется — ненадёжно |
| ≤1.5 ATR | 149 входов, +0.077R | +0.036R | +0.101R | первая половина после издержек в минусе |
| **≤1.2 ATR** | **55 входов, +0.264R** | **+0.381R** | **+0.220R** | **+0.291R и +0.149R после издержек** |

**Первый прогон только по одному дню на 48 бумагах дал ОТРИЦАТЕЛЬНОЕ ожидание
(−0.20R).** Мой вывод по одному Мечелу был выжившим — я нашёл единственного победителя и
построил под него правило. Только история и разбиение выборки показали правду.

**Чего проверка не доказывает:** выборка 55 входов, один режим рынка (июль 2026, рынок
рос), 12 бумаг из 48, тест на 10-мин свечах, а вживую сетап смотрит 5-мин. Порог выбран
после просмотра результатов. Это **измеряемая гипотеза**, не установленное преимущество.
Накапливайте исходы.

### Обязательный шаблон проверки любой гипотезы

1. Прогон на истории, а не на текущем дне. ISS даёт свечи: `iss.moex.com/iss/engines/stock/markets/shares/boards/TQBR/securities/{T}/candles.json?interval=10&from=&till=&start=`, порциями по 500 строк.
2. **Разбиение выборки по времени.** Если знак ожидания меняется между половинами — гипотеза не годится, какой бы хороший ни был средний результат.
3. **Издержки.** 0.05% на круг. При риске 0.66% это съедает 0.076R — достаточно, чтобы убить слабое преимущество.
4. Считайте, сколько конфигураций вы перебрали. Если положительна только одна из восьми — это шум.

---

## 6. Что открыто

**По убыванию ценности.**

1. **Telegram даёт ноль событий** при 21 настроенном канале, `api_configured=true`,
   `string_session=true`. Это главный текстовый источник. Мечел прошёл 7.1% за день, и в
   системе **ни одной строки новостей** по нему. Причина не диагностирована.
2. **Тянущийся стоп на трендовых днях.** Система не различает обычный день и трендовый.
   30.07 Мечел прошёл +11% от минимума на объёме 3.6×, а цели считались от ATR
   пятиминутки (0.5–0.85 ₽) — это масштаб обычного дня. Владелец держал и заработал
   вдвое больше, чем дал бы мой план. Распознавать трендовый день по темпу объёма и
   удалению от VWAP.
3. **Правило «стоп не ближе 0.5×ATR».** Мой стоп 39.10 стоял в 0.08 ₽ от цены при
   ATR 0.36 — внутри размаха одной свечи, и был выбит шумом. Уровень 39.18 не был бы
   задет. При размере владельца выбор уровня стопа стоит тысячи рублей.
4. **Генератор уроков выдаёт шаблон.** По сигналу 664 записал «сигнал держится на одном
   факторе без подтверждения техникой», хотя факторов было четыре. Настоящая причина
   провала — тесный стоп.
5. **Детектор режима ненадёжен.** SMLT помечен «range» при цене на 23% выше SMA20.
   Владелец решил оставить как есть: «если система определила тренд это уже хорошо».
   Прибор для замера построен: `GET /api/regime-audit`.
6. **Раскрытие эмитентов и календарь отчётностей не подключены.** Там лежат настоящие
   причины движений на 7%.
7. **9 тикеров без псевдонимов:** AFKS, BSPB, CBOM, EUTR, FEES, LSRG, MSNG, NKNC, UPRO.
8. **Новостной план: цель задана неверно.** Стоп стоит за ВСЕЙ свечой выноса (≈2.5×ATR)
   при цели 1.5×ATR, поэтому единый порог R/R обнулил бы ветку. Стоп должен стоять чуть
   за уровнем пробоя. Порог к этому плану намеренно НЕ применяется — см. `_plan(min_rr=None)`.
9. **Разрыв версий Python** между песочницей (3.9) и продом (3.11) — часть модулей
   локально не проверяется.

---

## 7. Ловушки — читать обязательно

Это самая полезная часть документа. Каждый пункт — моя ошибка этого дня.

### Данные

**Читайте незакрытую свечу как незакрытую.** Я посмотрел объём пробойной свечи Мечела,
увидел 0.9× от среднего и сказал владельцу «подтверждения объёмом нет». Свеча ещё
формировалась. Итог по ней — 2.9×. Совет был дан на неполных данных.

**Проверяйте имена полей, а не угадывайте.** За день я четыре раза запросил не те ключи:
`price_change_1d` вместо `change_1d`, `rsi` вместо `rsi14`, `items` вместо `events`,
`entry` вместо `entry_low`. Один раз это привело к выводу «лента пустая», хотя в ней было
200 записей.

**`bid_ask` — это отношение объёмов заявок, а не спред.** Настоящий спред в `spread_pct`.
Я передал отношение в риск-контур как процент и получил отказ по выдуманной причине:
«стоп 0.56% против спреда 1.70%» при реальном спреде 0.0257%. Ошибка в 66 раз.

**Смотрите на ленту, а не только на сводные поля.** По сводке POSI выглядел откатом в
восходящем тренде: ADX 32.6, цена выше обеих средних. По свечам — минимумы падали
980.8 → 971.2 → 970.6, цена ниже VWAP, объём на снижении растёт. Вывод из сводки был бы
неверным.

### Анализ

**Встречный поток при движущейся цене — это чаще поглощение, чем разворот.** Я дважды за
день прочитал «поток покупает при падающей цене» как давление продавца, и оба раза это
разрешилось вверх. Отличить можно только по следующим свечам: держится цена при
встречной дельте — поглощение; срывается на объёме — распределение.

**Одно наблюдение — не правило.** Мечел дал 4.03R, я построил под это сетап, а прогон по
48 бумагам показал −0.20R. Проверяйте на истории до того, как что-то зашивать.

**Не подгоняйте числа, чтобы сделка появилась.** Я один раз ужал стоп, пока R/R не стал
приемлемым. Риск-контур отклонил вход по спреду — и это было правильно, хотя причина
оказалась в моей же ошибке с полем.

### Инструменты

**Приборы уже построены — снимайте с них показания.** Дважды за день оказалось, что
диагностика существует, работает верно, а её вывод никто не сверял с тем, что реально
используется. `/api/health/figi` резолвил FIGI правильно, пока код брал чужие из
рукописной таблицы. То же с детектором режима. **Перед тем как строить прибор, проверьте,
нет ли его уже.**

**Молчаливый отказ хуже громкого.** Почти каждый дефект дня был невидим: коллектор
возвращал пустой список, исключения уходили в debug, статус брался из конфига вместо
измерения. Правило: **всегда записывайте причину отказа** и всегда помечайте основу
расчёта. Так сделаны `basis`, `age_min`, `stale`, `orb_blocked`, `breakout_blocked`,
`sblock`, `bblock`.

**Тесты на текст файла не проверяют импортируемость.** Синтаксическая ошибка в
`claude_agent.py` прошла набор из 172 тестов незамеченной, потому что тесты искали строки
в исходнике. Для этого добавлен `tests/test_imports.py` — он поймал ту же ошибку со
второго раза.

---

## 8. Справочник

### Наблюдение и управление

```
GET  /api/setup-watch              состояние наблюдателя + последние срабатывания
POST /api/setup-watch/start        запуск (interval_min)
POST /api/setup-watch/stop         остановка
POST /api/setup-watch/pass         один проход прямо сейчас (~18 сек на 48 бумаг)
GET  /api/live-signals             состояние сканера Claude + точность + калибровка
POST /api/live-signals/start       запуск (interval_min)   ВНИМАНИЕ: тратит бюджет
POST /api/live-signals/stop        остановка
GET  /api/ai/budget                расход за МСК-день
```

### Данные и диагностика

```
GET /api/screen?limit=48           триаж БЕЗ Claude, то есть бесплатно
GET /api/technical/{T}             дневная техника + trade_plan (ключи entry_low/entry_high, take_profit_1/2)
GET /api/technical/{T}/intraday    5-мин свечи, source, delayed
GET /api/feed?ticker=&kind=&limit= лента событий (ключ ответа events, НЕ items)
GET /api/health/sources            источники: RSS по каждому, telegram/pulse producing
GET /api/health/figi               ЖИВОЙ резолв FIGI в обход кэша
GET /api/orderbook-index           индекс по стакану
GET /api/regime-audit              прибор для замера детектора режима
GET /api/paper-account             виртуальный счёт по принятым сигналам
```

### Сигналы и журнал

```
POST /api/analyst-signal                    сигнал от агента: ticker, direction, entry,
                                            stop, target, confidence, reason, invalidation, analyst
POST /api/signals/{id}/decision             решение человека: decision=accept|reject|wait (query!)
POST /api/signals/{id}/correct              правка уровней ДО оценки
POST /api/signals/{id}/correct-position     правка снимка позиции, можно и после оценки
GET  /api/predictions?limit=                журнал с исходами
```

### Переменные окружения

Владелец правит сам в Coolify. Значение — слово `true`.

```
SETUP_WATCH_ENABLED=true        включён, автостарт наблюдателя
LIVE_SIGNALS_AUTOSTART=false    платный сканер, намеренно выключен
```

Верные по умолчанию, менять не надо: `SETUP_WATCH_INTERVAL_MIN=5` (один проход на
пятиминутный бар, чаще бессмысленно), `SETUP_WATCH_WARMUP_SEC=45`,
`SETUP_WATCH_DEDUP_MIN=30`, `BREAKOUT_MAX_WIDTH_ATR=1.2`, `SETUP_MIN_RR=1.5`,
`ORB_VALID_MIN=90`, `INTRADAY_MAX_AGE_MIN=40`, `INTRADAY_PRICE_MISMATCH_PCT=15`,
`SESSION_ALLOW_MORNING_ENTRY=true`, `LEARNING_AUTOSTART=true`, `DEMO_MODE=false`.

### Тесты

281 тест в 19 файлах, запуск `python3 tests/<файл>.py`. Прогоняйте **весь набор** перед
пушем: полный прогон трижды за день поймал регрессию, которую отдельный файл пропускал.

### Расписание торгов МСК

Утренняя 07:00–09:50, аукцион 09:50–10:00, основная 10:00–18:50, перерыв 18:50–19:00,
вечерняя 19:00–23:50. После 22:00 ликвидность падает (`SESSION_LOW_LIQUIDITY_AFTER`).
Утренняя сессия торгуется — раньше она попадала в фазу «closed», и три часа реальных
торгов пропадали.

---

## 9. Что делать первым

1. Снимите состояние: `/api/setup-watch`, `/api/health/sources`, `/api/ai/budget`.
2. Если наблюдатель не работает — запустите, при деплое он слетает.
3. Утром после 10:30 смотрите `recent` в `/api/setup-watch`: там срабатывания с входом,
   стопом, целью, R/R, шириной сжатия и кратностью объёма.
4. Каждый выданный сигнал — через `POST /api/analyst-signal`, иначе он не попадёт в
   обучение.
5. По списку открытого начинайте с Telegram: без текстовых источников новостная ветка
   стоит на трёх RSS-лентах, где Мечела за весь день не упомянули ни разу.

**И главное.** Владелец проверяет утверждения и ловит на них. За день он поймал меня на
неверной атрибуции результатов и на неполных данных, и был прав оба раза. Проверяйте
факты до того, как их произносить, и признавайте ошибку сразу — это работает лучше, чем
осторожные формулировки.
