# ─── RuBERT sentiment: отдельно от основного requirements.txt ─────────────────
#
# Вынесено 05.08. В образе эти два пакета занимали около 2.2 ГБ и дважды за
# вечер уронили сборку по нехватке диска на сервере. В торговом контуре
# тональность не участвует: сигналы строятся на объёме, стакане и цене.
#
# Установка для локальной работы с моделью:
#     pip install -r requirements.txt -r requirements-nlp.txt
#
# ВНИМАНИЕ ПРО torch. --extra-index-url НЕ задаёт приоритет источника: pip
# сливает оба индекса и выбирает по версии. Пин 2.4.1 без суффикса одинаково
# подходит и к 2.4.1 с PyPI (797 МБ + 12 пакетов nvidia-*, свыше 3 ГБ), и к
# 2.4.1+cpu (195 МБ). Если возвращаешь модель в образ — ставь torch ОТДЕЛЬНЫМ
# шагом Dockerfile с --index-url (без extra) и сначала убедись, что на сервере
# есть свободные три-четыре гигабайта.
--extra-index-url https://download.pytorch.org/whl/cpu
transformers==4.44.2
torch==2.4.1
