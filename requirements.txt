# ─── Core ─────────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.7.1
python-dotenv==1.0.1

# ─── Telegram ─────────────────────────────────────────────────────────────────
telethon==1.36.0
# Прокси для Telegram (SOCKS5/HTTP) на случай блокировки дата-центров Telegram
PySocks==1.7.1

# ─── HTTP / RSS ────────────────────────────────────────────────────────────────
httpx==0.27.0
aiofiles==23.2.1
# TLS-impersonating HTTP-клиент (обход анти-бот отпечатка Пульса T-Bank)
curl_cffi==0.13.0

# ─── База данных ──────────────────────────────────────────────────────────────
sqlalchemy==2.0.30
aiosqlite==0.20.0
asyncpg==0.29.0

# ─── Графики (для vision-анализа Claude) ──────────────────────────────────────
matplotlib==3.9.0
mplfinance==0.12.10b0
pandas==2.2.2

# ─── NLP (RuBERT sentiment) ───────────────────────────────────────────────────
# Без этих пакетов transformers.pipeline не импортируется и sentiment_analyzer
# молча падает на словарный fallback (keyword_sentiment).
# torch ставим CPU-сборкой с офиц. индекса PyTorch: дефолтное колесо с PyPI под
# Linux тянет CUDA (~2.5GB) и ломает slim-образ. На Linux встанет 2.4.1+cpu
# (~190MB), на macOS — обычный 2.4.1 (там он и так CPU).
--extra-index-url https://download.pytorch.org/whl/cpu
transformers==4.44.2
torch==2.4.1
