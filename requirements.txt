# ─── Core ─────────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.7.1
python-dotenv==1.0.1

# ─── Telegram ─────────────────────────────────────────────────────────────────
telethon==1.36.0
# Прокси для Telegram (SOCKS5/HTTP) на случай блокировки дата-центров Telegram
PySocks==1.7.1

# ─── HTTP / RSS ────────────────────────────────────────────────────────────────
httpx==0.27.0
aiofiles==23.2.1
# TLS-impersonating HTTP-клиент (обход анти-бот отпечатка Пульса T-Bank)
curl_cffi==0.13.0

# ─── Постоянное соединение с биржей (gRPC) ────────────────────────────────────
#
# Готового SDK нет: пакет tinkoff-investments снят с PyPI (404 на 01.08.2026),
# а оставшийся под похожим именем не обновлялся с 2021 года. Клиент генерируется
# из ОФИЦИАЛЬНЫХ контрактов биржи (proto/tinkoff, репозиторий
# RussianInvestments/investAPI), сгенерированное лежит в src/collector/tinkoff_pb.
# Перегенерация: make proto.
#
# Версии закреплены парой: сгенерированный код требует runtime protobuf не ниже
# того, которым сгенерирован. Меняешь одну — перегенерируй код.
# Оба пакета ставятся готовыми колёсами под cp311, компиляции в образе нет.
grpcio==1.80.0
protobuf==6.33.6
# даёт google/api/field_behavior.proto, на который ссылается marketdata.proto
googleapis-common-protos==1.72.0

# ─── База данных ──────────────────────────────────────────────────────────────
sqlalchemy==2.0.30
aiosqlite==0.20.0
asyncpg==0.29.0

# ─── Графики (для vision-анализа Claude) ──────────────────────────────────────
matplotlib==3.9.0
mplfinance==0.12.10b0
pandas==2.2.2

# ─── NLP (RuBERT sentiment): ВЫНЕСЕНО В requirements-nlp.txt 05.08 ────────────
#
# transformers и torch занимали в образе около 2.2 ГБ (колесо torch 195 МБ
# разворачивается в ~1.5 ГБ) и за один вечер дважды уронили деплой:
# сначала «no space left on device» на экспорте слоёв, затем pip убили на
# распаковке пакетов — exit 255 без единой строки ошибки.
#
# Почему можно: тональность в торговом контуре не участвует. Сигналы строятся
# на объёме, стакане и цене, вердикты сейчас делает агент, Claude отключён.
# SentimentAnalyzer без пакетов не падает: _load_sync ловит ImportError, а
# analyze_batch отвечает словарным keyword_sentiment.
#
# Нужна модель:  pip install -r requirements.txt -r requirements-nlp.txt
# Возвращаешь в образ: верни И шаг установки torch в Dockerfile
# (--index-url, не --extra-index-url), и проверь свободное место на сервере.
