"""
Зажигание: этот файл существует, чтобы workflow patch-apply запустился.

Запуск сборщика привязан к путям `scripts/_patch_*.py`. Прошлый прогон
применил патч сканера правильно, но упал на тестах, поэтому код не
закоммичен, а `scripts/_patch_scanner.py` остался лежать нетронутым.
Исправление живёт в `tests/conftest.py` — путь, который workflow не
сторожит. Чтобы аппликатор прошёл ещё раз по уже лежащему патчу, нужен
хоть один изменённый файл под сторожевым путём. Это он.

Скрипт ничего не правит и удаляет себя, чтобы не жить в репозитории
дольше одного прогона.
"""
import os

print("зажигание: правок нет, задача — разбудить прогон")
print("итог: применено 0, отказов 0")

try:
    os.remove(__file__)
    print("Скрипт удалил себя.")
except OSError as exc:                                       # noqa: BLE001
    print(f"удалить себя не вышло: {exc}")
