"""
Будильник применителя. Код не трогает.

patch-apply.yml срабатывает только на изменение scripts/_patch_*.py. Когда прогон
надо перезапустить — потому что тесты были красными или потому что упал сам
GitHub — одного пуша с тестами или кодом недостаточно, он его не разбудит.

Применитель прогоняет ВСЕ файлы scripts/_patch_*.py за один заход, так что
настоящий патч, лежащий рядом, применится заодно.

Повторное применение безопасно: каждая правка сначала смотрит, нет ли её
результата в файле, и говорит «уже было».
"""
import os

print("wake: будильник, код не трогал")

try:
    os.remove(__file__)
    print("wake: удалил себя")
except Exception as e:                                            # noqa: BLE001
    print(f"wake: себя не удалил: {e}")
