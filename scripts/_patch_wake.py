"""
Будильник применителя. Ничего не правит.

patch-apply.yml срабатывает только на изменение scripts/_patch_*.py. Когда
прошлый запуск упал на тестах, патч применился в ранере, но НЕ был
закоммичен — это защита, а не сбой. Чтобы повторить попытку после починки
теста, нужно изменение в этой папке — им служит этот файл.

Удаляет себя, чтобы не будить применителя при каждом следующем пуше.
"""
import os

print("wake: будильник, код не трогал")
try:
    os.remove(__file__)
    print("wake: удалил себя")
except OSError as e:
    print(f"wake: себя не удалил: {e}")
